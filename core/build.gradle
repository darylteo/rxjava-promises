import com.darylteo.gradle.plugins.maven.MavenPlugin

subprojects {
  apply plugin: 'java'
  apply plugin: MavenPlugin

  repositories { mavenCentral() }
  dependencies {
    testCompile 'junit:junit:4.11'
  }
}
